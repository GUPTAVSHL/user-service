server:
  port: 9002

spring:
  application:
    name: USER-SERVICE
  cloud:
    config:
      enabled: true
      uri: http://localhost:9190
  zipkin:
    base-url: http://localhost:9411/
  sleuth:
    enabled: true

